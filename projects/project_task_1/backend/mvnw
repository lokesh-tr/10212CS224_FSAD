#!/bin/sh
# Maven Wrapper script
# Downloads Maven if not present, then executes it

set -e

MAVEN_HOME="${HOME}/.m2/wrapper/dists/apache-maven-3.9.9"
MAVEN_URL="https://repo.maven.apache.org/maven2/org/apache/maven/apache-maven/3.9.9/apache-maven-3.9.9-bin.tar.gz"

if [ ! -d "${MAVEN_HOME}" ]; then
    echo "Downloading Maven 3.9.9..."
    mkdir -p "${MAVEN_HOME}"
    curl -fsSL "${MAVEN_URL}" | tar -xz -C "${MAVEN_HOME}" --strip-components=1
fi

exec "${MAVEN_HOME}/bin/mvn" "$@"
